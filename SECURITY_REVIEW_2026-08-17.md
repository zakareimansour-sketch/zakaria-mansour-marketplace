# مراجعة منصة ZAKARIA MANSOUR قبل استكمال التنفيذ

**التاريخ:** 2026-08-17  
**نوع المراجعة:** أمان، صلاحيات، بيانات، طلبات، ملفات، أداء، واستعداد للنشر  
**الحالة بعد الإصلاح:** اكتملت إصلاحات الأولوية الأولى بتاريخ 2026-08-17 واجتازت الاختبارات. تبقى الأولوية الثانية مطلوبة قبل النشر التجاري والدفع الحقيقي.

## تحديث تنفيذ المرحلة A

تم تنفيذ العناصر التالية بعد اعتماد المالك للتقرير:

- Rate limiting لتسجيل الدخول والتسجيل والإعداد والرفع والمكتبة والتحميل.
- نقل جلسة الواجهة من `localStorage` إلى Cookie آمنة `HttpOnly` في الإنتاج.
- إضافة Cookie CSRF منفصلة والتحقق منها في العمليات المعدلة للبيانات.
- تقييد CORS بقائمة نطاقات مسموحة مع دعم Credentials.
- رفض تشغيل Production دون JWT Secret قوي وAllowed Origins ورمز إعداد قوي عند الحاجة.
- قواعد انتقال صارمة لحالات الطلبات.
- إلغاء صلاحيات التحميل تلقائيًا عند الإلغاء أو الاسترجاع.
- عداد تحميل ذري يمنع تجاوز الحد في الطلبات المتزامنة.
- منع نشر أو بيع أو إتمام طلب لمنتج رقمي بلا ملف نشط.
- مسار واضح: إنشاء مسودة رقمية، رفع الملف، ثم النشر/المراجعة.
- توسيع الاختبارات إلى 8 اختبارات تشمل CSRF وحد المحاولات والاسترجاع وإبطال التحميل.


## الملخص التنفيذي

الأساس الحالي سليم كنسخة تطوير محلية، والاختبارات الحالية ناجحة. لا توجد ثغرات معروفة في الحزم وقت المراجعة، وقاعدة SQLite اجتازت فحص السلامة والعلاقات. لكن النسخة ليست جاهزة للنشر التجاري العام بعد؛ توجد ضوابط أمان وتشغيل يجب إضافتها قبل استقبال مستخدمين أو مدفوعات حقيقية.

لا توجد نتيجة في هذه المراجعة تقول إن المشروع تالف أو يحتاج إعادة بناء. المطلوب هو تقوية الأساس وفصل بعض الملفات الكبيرة قبل توسيع الخصائص.

## النتائج الآلية

- اختبارات API: **7/7 ناجحة**.
- بناء Production: **ناجح**.
- فحص `npm audit --omit=dev`: **صفر ثغرات معروفة**.
- SQLite `integrity_check`: **ok**.
- SQLite `foreign_key_check`: **لا توجد مخالفات**.
- عدد جداول التطبيق: **13**.
- عدد الفهارس المخصصة: **9**.
- لا توجد ملفات `.env` أو مفاتيح خاصة محفوظة داخل المشروع.
- لم يُعثر على `eval` أو `new Function` أو `dangerouslySetInnerHTML` في كود التطبيق.

## نقاط سليمة حاليًا

1. كلمات المرور مشفرة باستخدام bcrypt ولا تعاد في استجابات API.
2. صلاحيات الخادم تفصل العميل والبائع والإدارة والمالك.
3. إعداد المالك لا يعمل أكثر من مرة ويحتاج رمز إعداد.
4. منتجات البائعين لا تظهر قبل موافقة الإدارة.
5. المالك والمدير فقط يمكنهما النشر المباشر.
6. أسعار الطلب تؤخذ من قاعدة البيانات، وليس من السعر المرسل من المتصفح.
7. ملفات المنتجات الرقمية محفوظة خارج المسار العام.
8. روابط التحميل موقعة ومحدودة بخمس دقائق وعدد تحميلات.
9. صور المنتجات تتحول إلى WebP مع تحديد الأبعاد والحجم.
10. السلة تمنع جمع عملات مختلفة في طلب واحد.
11. الدفع عند الاستلام محظور على المنتجات غير المادية.
12. سجل تدقيق موجود للعمليات الحساسة الأساسية.
13. React يعرض نصوص المستخدمين بدون HTML خام، ما يقلل خطر XSS.

# إصلاحات الأولوية الأولى — قبل أي توسع أو نشر

## 1. حماية تسجيل الدخول والـAPI من المحاولات المتكررة

**المشكلة:** لا يوجد Rate Limiting حاليًا على تسجيل الدخول، إنشاء الحساب، إعداد المالك، رفع الملفات، إنشاء روابط التحميل أو AI مستقبلًا.

**الخطر:** تخمين كلمات المرور، إساءة استخدام التخزين، استنزاف الخدمة المجانية، وإرسال طلبات آلية كثيرة.

**الإصلاح المطلوب:**

- حد مستقل لتسجيل الدخول والتسجيل.
- حد للإعداد الأولي للمالك.
- حد لرفع الصور والملفات.
- حد لإنشاء روابط التحميل.
- حد للمساعد الذكي مستقبلًا.
- تسجيل المحاولات المرفوضة والتنبيه على النشاط غير الطبيعي.

## 2. طريقة حفظ جلسة المستخدم

**المشكلة:** رمز JWT محفوظ في `localStorage` داخل الواجهة.

**الخطر:** إذا ظهرت ثغرة JavaScript/XSS مستقبلًا، يمكن سرقة الجلسة.

**الإصلاح المطلوب قبل الإنتاج:**

- نقل الجلسة إلى Cookie آمنة `HttpOnly + Secure + SameSite`.
- إضافة Access Token قصير وRefresh Token قابل للإلغاء، أو جلسة خادم.
- إنهاء الجلسات عند تغيير كلمة المرور أو تعليق الحساب.
- إضافة حماية CSRF إذا استُخدمت Cookies.

## 3. CORS مفتوح أثناء التطوير

**المشكلة:** الخادم يقبل Origins ديناميكيًا في وضع التطوير.

**الإصلاح المطلوب:**

- قائمة محددة بنطاق الموقع في الإنتاج.
- رفض أي Origin غير معروف.
- فصل إعدادات التطوير عن الإنتاج.

## 4. دورة حالة الطلب وصلاحيات التحميل

**المشكلة:** الإدارة تستطيع حاليًا تغيير حالة الطلب إلى حالات متعددة دون قواعد انتقال صارمة. كما أن الإلغاء أو الاسترجاع لا يلغي صلاحيات التحميل تلقائيًا.

**الإصلاح المطلوب:**

- جدول انتقالات مسموحة للحالات.
- منع الرجوع من `completed` إلى `pending_payment` مثلًا دون إجراء إداري خاص.
- تعطيل صلاحيات التحميل عند `cancelled` أو `refunded` وفق السياسة.
- الاحتفاظ بسبب كل تغيير وحساب الموظف المنفذ.
- عدم اعتبار الطلب مدفوعًا إلا من Webhook موثوق أو إجراء مالك/مالية مسجل.

## 5. سباق عداد التحميل

**المشكلة:** فحص العدد ثم زيادته عمليتان منفصلتان منطقيًا؛ الطلبات المتزامنة قد تتجاوز الحد في حالات نادرة.

**الإصلاح المطلوب:**

- تحديث ذري بشرط `download_count < max_downloads`.
- عدم إصدار الملف إذا لم يغيّر التحديث صفًا واحدًا.
- التعامل مع فشل إرسال الملف وتحديد هل يحتسب أم لا وفق سياسة معلنة.

## 6. بيع منتج رقمي بلا ملف نشط

**المشكلة:** يمكن نشر منتج رقمي قبل رفع ملفه، ويمكن نظريًا إضافته إلى السلة.

**الإصلاح المطلوب:**

- السماح بالمسودة بدون ملف.
- منع النشر/الشراء للمنتج الرقمي إذا لم يوجد ملف نشط.
- إظهار حالة «غير جاهز للبيع» للمالك.
- إعادة فحص الملف عند إنشاء الطلب.

## 7. مفاتيح الإنتاج والإعداد الأول

**المشكلة:** في التطوير، يتم إنشاء `JWT_SECRET` ورمز إعداد عشوائيين وطباعتهما في السجل.

**الإصلاح المطلوب قبل النشر:**

- رفض تشغيل Production إذا لم توجد أسرار قوية في Environment Variables.
- عدم طباعة رمز الإعداد في سجلات الإنتاج.
- تعطيل مسار إعداد المالك نهائيًا بعد نجاح الإعداد.
- تدوير الأسرار مع خطة لا تُخرج المستخدمين بلا داعٍ.

# إصلاحات الأولوية الثانية — قبل الدفع الحقيقي

## 8. قاعدة البيانات والتخزين

SQLite والتخزين المحلي مناسبان للتطوير، لكنهما غير مناسبين لاستضافة Serverless متعددة النسخ.

المطلوب:

- PostgreSQL مستضافة أو خدمة مناسبة.
- Object Storage خاص للملفات الرقمية وعام للصور.
- نسخ احتياطي تلقائي ومختبر.
- Migrations مرقمة بدل الاعتماد فقط على `CREATE TABLE IF NOT EXISTS`.
- تشفير التخزين عند الراحة حسب إمكانات المزود.

## 9. تحقق أعمق من الملفات

الحماية الحالية تفحص الامتداد وبعض تواقيع الملفات وتمنع الملفات التنفيذية المعروفة، لكنها ليست مضاد فيروسات.

المطلوب:

- فحص Malware عند توفر خدمة مناسبة.
- حد لعدد الملفات ومساحة كل بائع.
- الحماية من ZIP bombs والملفات المضغوطة الضخمة.
- منع صيغ الماكرو مثل XLSM افتراضيًا.
- التحقق من ملفات Office المضغوطة قبل قبولها.
- سياسة حذف الإصدارات القديمة والملفات اليتيمة.

## 10. الدفع

قبل الربط الحقيقي:

- اختيار بوابة متاحة قانونيًا لحالة النشاط.
- التحقق من توقيع Webhook.
- Idempotency لمنع تكرار الدفع والطلب.
- تسجيل رقم عملية المزود دون حفظ بيانات البطاقة.
- تسوية ومراجعة مالية.
- سياسة Refund واضحة وربطها بصلاحية التحميل.

## 11. الحسابات

- تأكيد البريد الإلكتروني.
- استعادة كلمة المرور برمز محدود المدة.
- 2FA إلزامي للمالك والمدير المالي.
- سجل جلسات وأجهزة.
- قفل مؤقت بعد محاولات دخول فاشلة.
- تنبيه عند دخول إداري جديد.

# إصلاحات الأولوية الثالثة — جودة وقابلية الصيانة

## 12. تقسيم الملفات الكبيرة

- `src/main.jsx` تجاوز 55KB.
- `server/app.js` تجاوز 32KB.
- `src/styles.css` تجاوز 56KB.

قبل إضافة المحادثات والخدمات، يجب التقسيم إلى:

- وحدات Auth, Store, Cart, Dashboard, AI.
- Routes منفصلة للخادم.
- Services للطلبات والتحميل والتخزين.
- CSS أو Modules حسب كل مكون.

هذا يقلل أخطاء التعديل ويسهل الاختبارات.

## 13. الاختبارات الناقصة

الاختبارات الحالية قوية لمسارات MVP الرئيسية، لكن يلزم إضافة:

- انتهاء رابط التحميل.
- تجاوز عدد التحميلات.
- طلبان متزامنان للتحميل.
- إلغاء/استرجاع الطلب.
- ملف مزيف بامتداد مسموح.
- صورة أكبر من الحد.
- محاولات تسجيل دخول كثيرة.
- صلاحيات موظفي الدعم والمالية بالتفصيل.
- اختبارات متصفح فعلية للموبايل والكمبيوتر.
- قياس Coverage بدل عدد الاختبارات فقط.

## 14. الأداء

- إضافة Pagination للمنتجات والطلبات والمراجعات.
- عدم تحميل 200 طلب دفعة واحدة عند نمو المنصة.
- فهارس إضافية وفق الاستعلامات الفعلية.
- Lazy loading للصور والأقسام.
- تقسيم JavaScript إلى Chunks بدل ملف واجهة واحد.
- Cache مضبوط للصور مع حذف آمن عند استبدالها.

## 15. السياسات والامتثال

قبل استقبال مستخدمين حقيقيين:

- الخصوصية والشروط والاسترجاع.
- سياسة المنتجات الرقمية بعد التحميل.
- سياسة البائعين والمحتوى المحظور.
- حقوق الملكية الفكرية والإبلاغ.
- سياسة ملفات تعريف الارتباط إذا استخدمت Analytics أو مزودين خارجيين.
- مراجعة قانونية مناسبة للدول المستهدفة.

# القرار المقترح قبل المرحلة التالية

## المرحلة A — تقوية الأساس

1. Rate limiting وتسجيل محاولات الدخول.
2. قواعد انتقال الطلب وإلغاء صلاحيات التحميل.
3. تحديث ذري لعداد التحميل.
4. منع بيع منتج رقمي بلا ملف.
5. إعداد Production صارم للأسرار وCORS.
6. إضافة اختبارات الحالات السابقة.

## المرحلة B — إعادة التنظيم

1. تقسيم واجهة React إلى مكونات.
2. تقسيم API إلى Routes وServices.
3. إضافة طبقة إعداد موحدة للتطوير والإنتاج.

## المرحلة C — ثم نكمل الخصائص

1. المحادثات داخل الطلبات.
2. تذاكر الدعم.
3. مراحل تنفيذ الخدمات.
4. الكورسات.
5. ربط AI الحقيقي.
6. الدفع والنشر.

# النتيجة النهائية

**حالة النسخة المحلية:** جيدة وتعمل.  
**حالة الأمان للتطوير:** مقبولة مع نقاط معروفة.  
**حالة الاستعداد للنشر التجاري:** غير جاهزة حتى إنهاء الأولوية الأولى والثانية.  
**هل يمكن الاستمرار؟** نعم، بعد إصلاح المرحلة A أولًا، ثم تقسيم الملفات قبل إضافة وحدات كبيرة جديدة.
