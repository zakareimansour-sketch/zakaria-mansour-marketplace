# ZAKARIA MANSOUR Marketplace — ملف التسليم

## الحالة

- واجهة المتجر مكتملة كـMVP.
- نظام الحسابات والأدوار والبائعين والمنتجات والسلة والطلبات موجود.
- رفع الصور والملفات الرقمية والتسليم الآمن موجود.
- جلسات HttpOnly وCSRF وRate Limiting وقواعد الطلبات موجودة.
- قاعدة Supabase تم إنشاء مخططها بنجاح بواسطة المالك.
- Dockerfile وملف إعداد الإنتاج جاهزان.
- لا يوجد حساب مالك أو كلمة مرور مؤقتة داخل النسخة المسلّمة.

## سبب عدم وجود رابط دائم حاليًا

يلزم حساب استضافة خارجي صالح وقابل للدخول. محاولات Render وGitHub/Back4app تعطلت في المصادقة الخارجية. روابط Cloudflare السريعة مؤقتة ولا تصلح للإطلاق أو التسويق.

## الملفات المهمة

- `Dockerfile`: تشغيل المنصة في حاوية.
- `DEPLOYMENT.md`: متغيرات وإرشادات الإنتاج.
- `.env.example`: أسماء متغيرات البيئة دون أسرار.
- `supabase/schema.sql`: مخطط قاعدة Supabase.
- `server/`: الخادم والـAPI.
- `src/`: واجهة المتجر.
- `tests/`: اختبارات الخادم.
- `SECURITY_REVIEW_2026-08-17.md`: المراجعة الأمنية.

## أمر الاختبار

```bash
npm install
npm test
npm run build
```

## تنبيه

لا يجوز التسويق برابط Quick Tunnel مؤقت. يجب نشر Dockerfile على استضافة دائمة وضبط الأسرار والنطاق، ثم إنشاء حساب المالك من شاشة الإعداد الأولى.
